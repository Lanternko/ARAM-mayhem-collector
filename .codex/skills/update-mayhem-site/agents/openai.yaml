interface:
  display_name: "Update Mayhem Site"
  short_description: "Refresh, deploy, and recap the ARAM Mayhem tier-list website safely."
  default_prompt: "Refresh the ARAM Mayhem tier-list site using the project SOP, verify the generated docs/index.html, stage only the intended files, deploy, then summarize exactly what shipped."
